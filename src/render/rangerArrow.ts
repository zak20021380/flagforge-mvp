/**
 * Shared lightweight arrow proportions for the 6.1-unit Ranger. The assembled arrow is about 2.4
 * units long, matching the imported character's proportions without rendering the unsafe GLB mesh.
 */
export const RANGER_ARROW_VISUAL = {
  shaftLength: 2.05,
  shaftDiameter: 0.13,
  tipLength: 0.38,
  tipDiameter: 0.34,
  tipOffset: 1.19,
  fletchingLength: 0.34,
  fletchingWidth: 0.38,
  fletchingThickness: 0.075,
  fletchingOffset: -0.86,
} as const;
